{
    "name": "vizipedia-heroku",
    "version": "1.0.0",
    "scripts": {
      "start": "cd frontend && npm install && npm run build && cd .. && python backend/manage.py collectstatic --noinput && gunicorn backend.vizipedia.wsgi --log-file -"
    },
    "dependencies": {
      "serve": "^11.3.2"
    }
  }